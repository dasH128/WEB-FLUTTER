import 'package:flutter/material.dart';

class ProgramationsScreen extends StatelessWidget {
  const ProgramationsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.amber,
        child: Text('PROGRMATION'),
      ),
    );
  }
}
